# Database Configuration
DATABASE_URL=postgresql+psycopg2://mcpuser:mcppass@localhost:5432/mcpdb

# For Docker Compose (use this when running with docker-compose)
# DATABASE_URL=postgresql+psycopg2://mcpuser:mcppass@db:5432/mcpdb

# Gemini API Key used by the Vanna demo
GEMINI_API_KEY=